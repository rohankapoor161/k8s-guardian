apiVersion: v1
kind: Service
metadata:
  name: guardian-webhook
  namespace: guardian-system
  labels:
    app: guardian
spec:
  selector:
    app: guardian
  ports:
  - name: webhook
    port: 443
    targetPort: 8443
  type: ClusterIP
